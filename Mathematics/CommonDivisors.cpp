#include <bits/stdc++.h>

using namespace std;


int main() { //TODO: finish
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

#ifdef LOCAL
    freopen("../../input.txt", "r", stdin);
    freopen("../../output.txt", "w", stdout);
#endif

    int n, x;
    cin >> n;
    vector<int> nums;
    while (n--> 0) {
        cin >> x;
        nums.push_back(x);
    }


    return 0;
}